<h2>Lista de fundos: </h2>
<hr>

{{#each fundo}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>CNPJ: {{CNPJ}}</small><br>
            <small>Razão Social: {{razaoSocial}}</small><br>
            <small>Valor Unitário: R$ {{valorUnit}}</small><br>
            <small>Número de cotas: {{numeroCotas}}</small><br>
            <small>Preço médio: R$ {{precoMedio}}</small><br>
            <small>Retorno da operação: {{retOperacao}}%</small><br>
            <small>Saldo de aplicação do fundo: R$ {{saldoAplicFundo}}</small><br>
            <small>Data da Consulta: {{dataDaConsulta}}</small><br>
            <a href="/principal/fundos/comprar/{{id}}"><button class="btn btn-success mt-4">Comprar fundo</button></a>
            <div style='text-align:right'>
                <a href="/principal/fundos/vender/{{id}}"><button class="btn btn-danger">Vender fundo</button></a>
            </div>
        </div>
    </div>
{{else}}
{{/each}}
<br>

<div style="text-align:right"><a href="/principal/fundos/add"><button class="btn btn-success">Novo fundo</button></a></div>